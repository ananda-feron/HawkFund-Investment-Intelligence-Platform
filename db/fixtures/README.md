# Phase 0 fixtures

`load.py` creates one fund, three role-scoped development users, and four reference instruments. IDs and timestamps are fixed, and every insert is idempotent.

It intentionally creates no positions, transactions, prices, risk results, scenarios, proposals, or AI data.
