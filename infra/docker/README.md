# Docker infrastructure

The root `docker-compose.yml` is the development stack's canonical definition. This directory is reserved for container-specific documentation and future environment overrides; Phase 0 deliberately keeps the local topology in one file.
